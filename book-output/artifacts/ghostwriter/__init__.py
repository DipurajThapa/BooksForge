"""Ghostwriter-grade pipeline for BooksForge.

Modules:
  anti_ai_tells       — pattern-based AI-prose detector + revision-prompt builder
  voice_fingerprint   — measurable voice profile + stylometric distance
  genre_packs         — literary / genre / non-fiction system prompts + rubric weights
  pipeline            — orchestrator that ties them together
"""
