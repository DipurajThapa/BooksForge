// @booksforge/preview — canonical-HTML preview renderer.
// Full implementation in M5. See EXPORT_EPUB_SPEC.md.
//
// The preview renders the same canonical HTML that feeds the EPUB packager.
// Any visual drift between preview and EPUB is a CI failure.
//
// This package will export:
//   PreviewPane  — React component rendering canonical HTML in an iframe
//   usePreviewUrl — hook that keeps the preview URL in sync with editor state

export {};
