# Rust
target/
# Cargo.lock IS committed for this binary workspace (Tauri desktop app);
# do not ignore it. Mirrored guarantee in the root .gitignore.

# Node / pnpm
node_modules/
.pnpm-store/

# Tauri build outputs
apps/desktop/src-ui/dist/
apps/desktop/src-ui/.vite/
apps/desktop/gen/

# Playwright run artefacts (visual-regression suite)
tests/visual-regression/playwright-report/
tests/visual-regression/test-results/

# TypeScript bindings are regenerated by `cargo test -p booksforge-ipc`
# and committed so CI can detect Rust↔TS drift.

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp

# Environment
.env
.env.local
*.pem

# BooksForge project bundles created during development
*.booksforge/
